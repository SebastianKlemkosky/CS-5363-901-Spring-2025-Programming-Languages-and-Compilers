double Pi() {
    return 3.14159;
}

double Multiply(double x, double y) {
    return x * y;
}

void main() {
    double a;
    double b;
    double result;

    a = Pi();
    b = 2.0;
    result = Multiply(a, b);

    // These Print calls use only supported types (int + string)
    Print(1, " done.\n");
}

